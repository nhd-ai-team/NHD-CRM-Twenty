# 02 · 当前进度与状态

最后更新：2026-08-19

---

## 已完成 ✅

### 基础设施与基础能力

| 事项 | 状态 | 说明 |
|---|---|---|
| Twenty 部署 | ✅ | macmini Docker（compose 项目名 `twenty`），公网经 Cloudflare 隧道 `crm.chinanhd.com` |
| 中文界面 | ✅ | 已切简体中文 |
| AI 模型接入 | ✅ | OpenRouter（Claude/GPT/Gemini），走代理 |
| 邮箱接入 | ✅ | `sales@chinanhd.com`，IMAP/SMTP，已归档 590+ 封 |
| 数据备份 | ✅ | 存于 `backup/` |

### 对话工作台 / 全渠道会话

| 事项 | 状态 | 说明 |
|---|---|---|
| 对话工作台（/chat） | ✅ | 聚合 WhatsApp / 官网客服会话，支持接管、AI 托管、转线索 |
| WhatsApp 多账号绑定 | ✅ | 每销售独立 WAHA Session，互相隔离；自助绑定 UI 在「设置→我的渠道」 |
| 会话权限 RBAC | ✅ | 销售看本人 / 主管看团队 / 管理员看全部 / boss 仅查看；前端权限卡片可配置 |
| 会话归属与转交 | ✅ | owner_id 维度隔离，支持离职转交、历史保留 |
| 审计日志 | ✅ | 登录、绑定/解绑、查看、发送、权限变更、会话自动释放 |
| 官网客服 Widget | ✅ | 官网嵌入 JS，AI 自动回复 + 人工接管，自动建线索 |
| 官网表单 → 线索 | ✅ | POST 自动建「未处理线索」，三表同源、leadNo 兜底 |
| 对话名称编辑与持久化 | ✅ | 人工改名持久化，webhook 不再覆盖（2026-08-18） |
| WhatsApp 送达/已读回执 | ✅ | `message.ack` 升级 sent→delivered→read（2026-08-18） |
| 官网访客地域识别 | ✅ | IP 仅审计，geo_* 推断国家/地区（2026-08-18） |

### 会话治理（2026-08-18 新增/修复）

| 事项 | 状态 | 说明 |
|---|---|---|
| 接管「死锁」修复 | ✅ | 官网 open 会话任何时段可接管，按钮不再因排班置灰 |
| 接管超时自动释放 | ✅ | 官网 takeover 连续 120min 无人工消息→自动释放为 open |
| 线索 ID(leadNo) 必填兜底 | ✅ | DB 触发器 + 三入口显式生成，历史缺失行已回填 |
| 线索初始阶段规则 | ✅ | 仅官网表单=未处理线索；官网客服/WhatsApp 等其他=线索 |

### 沟通状态表单与协办人（2026-08-18 实施）

| 事项 | 状态 | 说明 |
|---|---|---|
| 沟通状态对象（duiHuaLiShi） | ✅ | 会话档案对象，1 会话=1 记录，字段含渠道/状态/结果/负责人/摘要/消息数/conversationId |
| 沟通状态负责人关系 | ✅ | `conv.owner_id` 保存 userId；同步到沟通状态时转换为 `workspaceMember.id` 写入负责人关系字段，避免关系字段写入失败 |
| 协办人（xieBanRenId） | ✅ | middleware 通用端点 + UI 悬浮按钮/弹窗；owner/admin 可设，boss 只读（详见 docs/20） |

### 注入层重构（2026-08-19，详见 docs/21）

| 事项 | 状态 | 说明 |
|---|---|---|
| 页面切换卡死修复 | ✅ | `findRightDrawer` 宽泛选择器收窄 + MAX_SCAN 上限（v5） |
| tick 调度根因重构 | ✅ | 200ms 时间节流 + 功能注册表 FEATURES + 路由 gate（root-v1） |
| 统一路由解析 | ✅ | `parseRoute()`/`canonicalObject()` 单一事实源，消灭散落正则 |
| 全文档扫描清除 | ✅ | `applyLeadCompanyUiFix` 去 setInterval 每秒全扫，仅机会页按需扫 |
| chat-nav.js 模块化 | ✅ | 3001 行单文件 → `nginx/inject/src/*.js`（9 模块）+ `build-chat-nav.js` 构建 |
| 沟通明细内联弹窗 | ✅ | 「查看对话内容」不再跳全屏 iframe，就地弹窗渲染消息文本 + 关键词过滤（inline-chat-v1） |
| 钻取按钮残留修复 | ✅ | 移除 S3 宽泛兜底（inline-chat-v2）；对话工作台/邮件 iframe 激活时强制移除（inline-chat-v3/v4） |
| 线索页崩溃修复 | ✅ | 注入脚本 DOM 修改与 React 渲染错开：applyLeadCompanyUiFix 改 120ms 防抖（v5）；scheduleTick 全异步 tick（v6）——见 docs/21 §1.7 |
| 浏览器端错误诊断 | ✅ | 注入层记录运行时错误/未处理 Promise 到 `window.__NHD_ERRORS__`，用于定位设置页、线索页、工作台空白或 React 错误边界 |

### 对话工作台体验（2026-08-19）

| 事项 | 状态 | 说明 |
|---|---|---|
| IP 地域定位修复 | ✅ | 内置默认 GeoIP provider（ip-api.com）+ ai-service 透传访客真实 IP（X-Forwarded-For），官网会话地域正常显示；历史内网 IP 无法回填（本就非访客真实 IP） |
| 官网渠道已读未读 | ✅ | 访客发下一条消息 = 已读之前我方出站消息（delivery_status→read + read_at）；前端 website 渠道展示 已发送/已读 |
| 地域显示优化 | ✅ | 列表/详情去掉时区字段；地域行仅官网渠道展示（WhatsApp/邮件无 IP 概念，避免「未知地区」噪音） |

---

## 进行中 / 观察中 🟡

| 事项 | 状态 | 说明 |
|---|---|---|
| 邮件增量同步 | 🟡 运行中 | 新邮件自动进来，无需干预 |
| 客户联系人持续生成 | 🟡 运行中 | 随邮件增量自动创建 |
| 真实渠道人工验证 | 🟡 待补 | WAHA 回执/名称覆盖、接管 UI 不再置灰，建议真实渠道走一遍终验 |
| M7 安全测试 | 🟡 待做 | 销售只看自己、admin 看全部、禁跨绑、禁越权（见 docs/12） |

---

## 未开始 / 规划中 ⬜

| 事项 | 说明 |
|---|---|
| 邮箱多账号绑定 | 企业邮箱单一，Twenty 原生共享收件箱够用，暂缓 |
| 多租户隔离 / WhatsApp 群聊 / 状态广播 | 本期不做（见 `docs/12`） |
| Workflow 自动化 | 自动跟进、自动分配等 |
| 触发器在 Twenty 大版本重建表后的重跑 | `opportunity_leadno_default_trg` 为数据层对象，重建表需重跑 `middleware/sql/leadno_default_trigger.sql` |

---

## 关键数据快照（截至 2026-08-18）

- 线索(leadNo) 全库 NULL 数：**0**（触发器兜底 + 历史回填后）
- 官网表单来源线索：未处理线索（WEI_CHU_LI_XIANSUO）保持原样
- 官网客服来源线索：已无「未处理线索」，统一为「线索(XIANSUO)」
- 已接入邮箱：`sales@chinanhd.com`
- 运行环境：macmini，Docker compose 项目名 `twenty`
- 当前 workspace 数据库 schema：`workspace_3zyju8y4v9gnoifvksi4cn23f`
  （注：每次全新重装会变化，查数据时以实际为准）

---

## 环境信息

| 项 | 值 |
|---|---|
| 公网访问地址 | https://crm.chinanhd.com（Cloudflare 隧道 → portal nginx :3000 → middleware :3002）|
| 本地访问地址 | http://localhost:3000（Twenty）/ http://localhost:3002（middleware 本机接口）|
| 容器运行时 | Docker（macmini） |
| 数据库 | PostgreSQL（容器 twenty-db-1，schema 见上）|
| 缓存/队列 | Redis（容器 twenty-redis-1，含元数据缓存）|
| 应用 | twenty-server-1 / twenty-worker-1 / twenty-twenty-portal-1 / twenty-middleware-1 / twenty-waha-1 |
| WhatsApp 网关 | WAHA（容器 twenty-waha-1，事件 `message.any` 含 ack 子事件）|
