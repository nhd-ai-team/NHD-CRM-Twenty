# 13 · 后续开发计划（PRD 遗留 + WK2 会议新增）

> 整理日期：2026-08-07（首版）；2026-08-07 二次更新——按最新优先级澄清重排（本次为重建版，原文件因会话内沙箱故障丢失后按记忆重写，内容一致）
> 来源：`docs/PRD.html` 现有条目 + 《CRM 周度进度同步_WK2》会议纪要 + 会后优先级澄清
> 目的：区分「PRD 原本就计划要做、还没做完的」和「本次会议新提出/需要重新对齐的」，避免混为一谈、重复设计。

---

## ✅ P0：附件收发闭环（已完成，2026-08-07）

- WhatsApp 入站附件保存与展示：代理下载 WAHA 媒体到本地存储（commit `df586a8`）
- 官网附件跨域打开报错：附件下载路由补 `Content-Disposition`（commit `81b2ab0`）
- 官网访客通过 widget 发送附件：ai-service 新增上传接口 + widget.js 加附件按钮 + middleware 接收入库（commit `df101c3` + ai-customer-service-system 仓库 `d036200`），全链路已用真实浏览器验证通过

---

## 🔴 P1：需求已明确，优先排期（2026-08-07 澄清）

| 点 | 需求说明 | 备注 |
|---|---|---|
| **附件按线索维度跟踪** | 跟进模块本身**不需要**上传附件功能；但需要把一个线索整个聊天过程中（可能横跨 WhatsApp、官网等多个渠道会话）收发过的所有附件，按**线索**维度汇总展示，方便回溯"这个客户前后发过哪些文件"。 | 技术路线可参考本次会话刚做完的「跟进记录」入口——同样是线索详情页悬浮入口 + 弹窗列表，按 `syncGroupCode`/conversation→contact 关联汇总跨渠道数据；区别是这次汇总的是 `conv.messages` 里带 `attachments`/`media_url` 的消息，不是 `conv.follow_ups`。待排期实现。 |
| **WhatsApp 主动建会话方式调整** | 按客户方要求的方式来——即"直接搜号码建会话"，而不是我们原设计的"必须先收到对方消息才能建"。 | 需要重新设计这部分交互和后端逻辑，原设计（`docs/09-WhatsApp主动新建会话第一性原理与对抗性审查.md`）需要重新评估调整。 |

---

## 🟠 P2：本期要做（2026-08-07 澄清）

| 点 | 需求说明 |
|---|---|
| **忘记密码 / 管理员重置密码** | 本期核心目标之一。Twenty 原生没有这个能力（查过源码 `twenty/server/src/core/auth`，没有 forgot-password/reset-password resolver），需要自研——用户忘记密码时，管理员能在后台帮其重置。 |
| **沟通状态菜单改表单形式呈现与优化** | 当前「沟通状态」（原「对话历史」，Twenty 原生对象 `duiHuaLiShi`）的展示需要改成以表单形式呈现，并做优化。具体表单字段/交互需要进一步对齐细节后再排期实现。 |

### 本期明确不做 / 冻结

| 点 | 处理方式 |
|---|---|
| **复杂 AI 配置**（兜底话术/知识库范围/敏感词/允许问题类型） | 本期不做，移出当前迭代范围。 |
| **WhatsApp 多账号绑定 + RBAC** | **暂时封版到当前状态**——不再继续往下做，保持现状，不纳入本期排期。 |

---

## 🟡 P3：其余 PRD 遗留（暂不排期，按需再启动）

| 功能点 | 依据 |
|---|---|
| 消息内搜索 | PRD："暂不作为 v1.0 必交付；后续在沟通状态模块统一设计" |
| 关键词高级筛选 | PRD："高级筛选作为后续待办" |
| 链接富预览 | PRD："富预览作为后续增强项" |
| 客户跨平台身份合并（无 syncGroupCode / 无官网URL 的情况） | 已讨论过，用户明确"这个情况再说吧"，暂缓 |
| 项目成员可见范围（行级权限） | 从 07-27 起一直是待办，未排期 |
| 前端字段全面中文化 | 之前尝试失败过；⚠️ 做的时候必须连带改 `conv.sync_from_*` 三个触发器函数里的列名（参见本次会话修过的 person.country→guoJia 断链故障） |
| 对话历史板块 1.1（自研表+owner过滤） | mockup 已产出，代码未动；跟"沟通状态菜单表单化"这条要合并考虑，避免重复设计 |
| 鉴权升级（Cloudflare Access 域级门禁） | 未做 |
| "转客户"按钮技术债（DOM 注入 → Twenty 原生 React 组件） | 纯技术债，不影响功能，PRD 里明确写了后续要迁移 |
| 企业账号升级（WhatsApp/Instagram/Facebook Business API） | PRD："待后续单独评估，不纳入 v1.0 范围" |
| 多坐席复杂排班 | PRD："当前团队规模不需要，后续视情况扩展" |

---

## ⚪ 待验证 / 待复现（不一定是真问题）

| 点 | 说明 |
|---|---|
| 官网多语言自动回复演示时偶发无响应 | 演示现场出现过，怀疑走了翻译链路的问题，未定性，需要复现排查 |
| WhatsApp 绑定掉线 | 提到"独立板块容易掉线"，需要实测 |
| 多端登录是否冲突 | 客户方提问，回答"应该没问题"但承认"没考虑这个"，需要验证 |

---

## 跟已在推进的方向对应关系

- **Opp/People/Project 三方数据整合**：1.0（强标识符精确匹配）已实现并验证通过；2.0（模糊打分匹配）暂缓，"没有 syncGroupCode 关联也没填官网URL"的情况用户明确"这个情况再说吧"。
- **附件按线索维度跟踪**（本次新增 P1）跟"跟进记录汇总"是同一套技术模式（线索详情页悬浮入口 + 按 syncGroupCode/conversation 关联跨渠道汇总），实现时可以直接复用/参考，不要另起一套设计。
