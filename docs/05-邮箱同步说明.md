# 05 · 邮箱同步说明

最后更新：2026-09-07

---

## 现状

- 已接入邮箱：`sales@chinanhd.com`（网易企业邮箱 / 灵犀办公）
- 同步方式：IMAP（收）+ SMTP（发）
- 已归档邮件：590+ 封
- 自动生成联系人：286+ 个
- 状态：✅ 正常，新邮件自动增量同步
- 邮件视图：收件箱、发件箱、垃圾邮件、全部四个独立分类；按会话是否包含对应类型邮件判断，同一往来线程可同时出现在收件箱和发件箱，详情逐封显示方向。
- 邮箱来源分类：默认同步 `INBOX`、已发送、垃圾邮件；地区分类文件夹通过 `IMAP_SYNC_MAILBOXES` 可按批次加入。垃圾邮件保留并标记，默认收件箱隐藏，可在“垃圾邮件”分类查看。

---

## 邮件在哪看

Twenty 是 **CRM**，邮件不是独立的"收件箱"，而是**挂在对应客户名下**：

1. 进 **People（联系人）** 或 **Companies（公司）**
2. 点开某个客户
3. 客户详情页里的 **Emails / 时间线** 区域，就是跟这个客户的邮件往来

> 逻辑：邮件按"发件人邮箱"自动匹配到对应联系人。所以联系人也是随邮件自动生成的。

### 如何区分收发

- 邮件列表提供“收件箱”和“发件箱”独立入口，不再依赖每封邮件旁的方向标签。
- 邮件详情同时显示发件人、收件人和抄送人，避免只看联系人名称产生误判。
- 邮箱列表按邮件会话分页加载，顶部数量取数据库汇总值，滚动到底部会继续加载；该数量不是当前已加载的首屏条数。
- 收件邮件方向来自 IMAP 文件夹；CRM 中已记录的出站邮件按消息方向显示为“发件”。

---

## 连接配置（如需重新配置时参考）

| 项 | 值 |
|---|---|
| 邮箱地址 | sales@chinanhd.com |
| IMAP 服务器 | imap.qiye.163.com |
| IMAP 端口 / 加密 | 993 / SSL/TLS |
| SMTP 服务器 | smtp.qiye.163.com |
| SMTP 端口 / 加密 | 465 / SSL/TLS |
| 密码 | **客户端授权码**（不是登录密码）|

> 授权码在网易企业邮箱后台生成：账号与安全 → 客户端授权密码。

---

## 关键设置

### 同步来源文件夹
- 默认同步 **INBOX、已发送、垃圾邮件**；地区分类文件夹（01_中东 ~ 08_未识别地区）建议在完成核心同步后分批加入 `IMAP_SYNC_MAILBOXES`。
- **排除**：草稿箱、已删除
- 已发送和垃圾邮件会同步并保留来源元数据；垃圾邮件默认不进入收件箱视图，可在“垃圾邮件”分类查看。

### 自动创建联系人策略
- 设置在：设置 → 账户 → 电子邮件
- 当前策略：**发件和收件（SENT_AND_RECEIVED）**
- 效果：给你发过邮件的客户，自动生成为 People 联系人

---

## ⚠️ 网易限流（最重要的注意点）

网易企业邮箱对第三方 IMAP 客户端有**严格频率限制**：

- **不要反复断开/重连账户** → 会触发限流
- 触发后现象：连不上、"收不到问候语超时"、同步卡住
- 触发后处理：**等 15-30 分钟冷却**，别急着重试
- **平时正常同步不受影响**，限流只针对"频繁新建连接"

**一句话：现在正常了，就别去动它。** 新邮件会自动进来。

---

## 已知限制

- ❌ **不能在 Twenty 界面直接写邮件/群发** —— 回复会跳回你的邮箱客户端
- 批量开发信需外接专业邮件营销工具（Mailchimp/SendGrid 等）
- 邮件是按"客户"维度组织的，没有传统"统一收件箱"视图

---

## 排查同步问题

```bash
cd "/Users/nhdailabcenter/Desktop/some agents/tools-claude/ai crm"

# 看邮件同步日志
docker compose logs worker --tail=50 | grep -i imap

# 查已入库邮件数（schema名以实际为准，见 docs/02）
docker exec twenty-db-1 psql -U postgres -d default -t \
  -c 'SELECT COUNT(*) FROM "workspace_3zyju8y4v9gnoifvksi4cn23f"."message";'
```
