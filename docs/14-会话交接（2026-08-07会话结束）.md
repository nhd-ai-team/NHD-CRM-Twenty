# 14 · 会话交接文档（2026-08-07 会话结束）

> 用途：给新开的 session 快速接续上下文。本次会话做了什么、验证到什么程度、有什么已知坑，都在这里。

---

## 本次会话做完并已提交的事

所有 commit 均在 `ai crm` 仓库 main 分支，**已 push 到 origin/main**（push 到 `6a5e291` 那次，后面几个新 commit 还未确认 push，见下方「待办」）。

| commit | 内容 |
|---|---|
| `4ab5022` | 修复三表同步触发器 person 字段改名（`country`→`guoJia`）导致的断链故障，Opportunity/People/Project 三表字段互补链路重新跑通 |
| `d373e3a` | 修复对话工作台当前会话不实时刷新（轮询逻辑只刷列表不刷当前打开的会话消息） |
| `1ae65eb` | 合并了用户本地并行做的 WhatsApp 多账号 RBAC、跟进记录权限、最新跟进字段等改动（这部分不是本会话写的，是审查+一起提交） |
| `df586a8` | 修复 WhatsApp 入站附件无法展示：WAHA 媒体 URL 的 host 不可信（泄露了内部端口 localhost:3000），改成代理下载到本地存储 |
| `123afff` | 新增线索详情页「跟进记录」悬浮入口，汇总全渠道跟进，按角色权限过滤 |
| `6a5e291` | 简化转线索提示文案 + 首版开发计划文档（**这个文档后来被删了，见下方**） |
| `81b2ab0` | 修复官网附件跨域无法打开：附件下载路由补 `Content-Disposition` 头 |
| `df101c3` | 支持官网访客在 widget 里发送附件（新功能，另有配套 commit 在 `ai-customer-service-system` 仓库） |

**另一个仓库**（`/Users/nhdailabcenter/Desktop/测试/ai-customer-service-system`，独立 git 仓库，非本仓库子目录）：
- commit `d036200`：官网 widget.js 支持访客上传附件（后端 base64 上传接口 + 前端附件按钮 + `ChannelMessageEvent` frozen dataclass 踩坑修复）

## 本次会话验证过的关键事实

- **附件收发闭环（P0）已基本完成**：WhatsApp 入站附件、官网附件跨域打开、官网访客发附件，三个链路都用真实浏览器/真实数据验证过（细节见对应 commit message）。
- **Opp/People/Project 三方数据整合 1.0 已验证跑通**：靠 `syncGroupCode`（隐藏业务编码，线索创建时自动生成）+ Postgres 触发器做三表字段互补；靠 `conv.normalized_website_domain()` 域名归一化做跨业务链同公司归纳。**不是打分制**，1.0 是强标识符精确匹配。2.0 打分制模糊匹配暂缓（用户原话"这个情况再说吧"）。
- **一个重要教训**：在 Twenty UI 里改一个参与三表同步的字段名（比如 People 的 `country`→`guoJia`），**会静默炸掉整条同步触发器链路**，且前端只报"Data validation error"看不出根因，得去 `docker compose logs server | grep "query failed"` 才能查到。用户决定暂不做防护，以后改名时要记得连带更新 `conv.sync_from_*` 三个函数。

## 已知问题 / 未完全验证的地方

1. **middleware/index.js 有大量用户本地并行开发的 WhatsApp 多账号 RBAC 改动**，已经在 `1ae65eb` 里一起提交过一次；如果新 session 开始时发现 `git status` 又有 middleware 的未提交改动，大概率是用户自己在改，**默认不要动，先问**。
2. **附件相关的几个修复都是我自己验证的，没有用户真实点击确认过**（尤其 WhatsApp 附件那个，请用户找人发一张图片/语音实测）。
3. **本次会话末尾遇到过一次奇怪的沙箱权限故障**：整个工作目录突然对 Bash/Read/Write 全部返回 `Operation not permitted`（`docs/13-....md` 这个文件最先出现，后来蔓延到整个目录），用户在自己电脑上确认目录正常能打开，过了一段时间后自动恢复正常，原因不明，怀疑是本会话沙箱的临时故障，不是文件系统真实损坏。**如果新 session 又遇到类似情况，不用怀疑是文件权限问题，等等看或者提醒用户重开 session**。
4. **`docs/13-后续开发计划（PRD遗留与WK2会议新增）.md` 这个文件已被删除**，原因就是上面第 3 点的故障导致读写不了，用户让删掉重建。**新内容见 `docs/13`（本次会话结尾会重新创建），如果发现 `docs/13` 不存在，需要重新整理一份**——最新版优先级见下一节。

## 下一步要做的事（已经和用户对齐过优先级，2026-08-07 澄清版）

详见新版 `docs/13-后续开发计划（PRD遗留与WK2会议新增）.md`（本会话结尾会重建），概要：

- **P1（优先排期）**：① 线索维度的附件汇总跟踪（参考"跟进记录"那个悬浮入口+弹窗的实现模式）② WhatsApp 主动建会话方式按客户要求调整为"直接搜号码"
- **P2（本期要做）**：① 忘记密码/管理员重置密码（Twenty 原生没有，需自研）② 沟通状态菜单改表单形式呈现与优化
- **P2 明确不做/冻结**：复杂 AI 配置本期不做；WhatsApp 多账号 RBAC 暂时封版到当前状态，不再继续往下做
- 其余 P3/待验证项见文档

## 关键环境信息（新 session 如果没有历史 memory 会用到）

- 仓库根目录：`/Users/nhdailabcenter/Desktop/some agents/tools-claude/ai crm`
- 另一个独立仓库（ai 客服/官网 widget）：`/Users/nhdailabcenter/Desktop/测试/ai-customer-service-system`
- workspaceId：`438bc184-abf4-4992-8de4-c2669c4ca65b`；workspace schema：`workspace_3zyju8y4v9gnoifvksi4cn23f`
- 改 Twenty metadata（字段/视图）后必须清 Redis 元数据缓存 + 重启 server/worker/twenty-portal 才生效：
  `docker exec twenty-redis-1 redis-cli --scan --pattern "engine:workspace:*<workspaceId>*" | xargs redis-cli DEL`
- middleware 改动部署：`docker compose build middleware && docker compose up -d middleware`（ai-service 同理）
- 更多背景见 memory（project_crm_snapshot / project_opp_people_project_sync / project_todo_backlog / project_conversation_history）
